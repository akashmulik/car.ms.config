spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/carappointment
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

logging:
  file:
    name: /home/pi/LOGS/car-appointment.log
